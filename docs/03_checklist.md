# Claude Code 設計チェックリスト

最終更新: 2026-06-08

---

## プロジェクト初期設定

- [ ] `/init` で CLAUDE.md を自動生成（`CLAUDE_CODE_NEW_INIT=1` でインタラクティブ版）
- [ ] CLAUDE.md を 200行以内に保つ（超えると指示が無視されやすくなる）
- [ ] `.claude/rules/` でパススコープルールを整理（コンテキスト節約）
- [ ] 権限設定: `/permissions` で allowlist を設定、または Auto Mode を検討
- [ ] フックで必須チェックを自動化（eslint、typecheck等）

---

## サブエージェント設計

- [ ] `description` は Claude が自動委任できる十分に具体的な説明か
- [ ] `tools` は最小限の権限に絞っているか
- [ ] 調査系タスクには `haiku`、品質系には `opus` を指定したか
- [ ] 同じワーカーを繰り返し使う場合は `.claude/agents/` に定義したか
- [ ] 調査にはサブエージェントを使い、メインコンテキストを守っているか

---

## スキル設計

- [ ] SKILL.md frontmatter に `name` と `description` があるか
- [ ] 副作用のあるワークフローは `disable-model-invocation: true` を設定したか
- [ ] CLAUDE.md に毎回入れる必要のない情報はスキルに移動したか
- [ ] スキルは単一責任（一つのことだけ）になっているか

---

## フック設計

- [ ] 危険なコマンドは `PreToolUse` でブロックしているか
- [ ] ファイル編集後に必要なチェック（lint/typecheck）は `PostToolUse` に設定したか
- [ ] `SessionStart` で開発コンテキストを自動ロードしているか
- [ ] exec形式（`args: string[]`）を使い安全なコマンド起動にしているか（v2.1.139+）
- [ ] `continueOnBlock: true` でブロック理由を Claude にフィードバックしているか（v2.1.139+）
- [ ] `once: true` で初期化フックがセッション中に1回だけ実行されるようにしているか（2026-05-18+）
- [ ] 長時間バックグラウンド処理に `asyncRewake: true` を使っているか（2026-05-18+）
- [ ] 並列ツール呼び出しの後に検証が必要なら `PostToolBatch` を使っているか（2026-05-18+）
- [ ] 外部エンドポイントへのHTTPフックに `allowedEnvVars` で認証情報を渡しているか（2026-05-18+）
- [ ] 全フックを無効化したい場合に `disableAllHooks: true` を知っているか（2026-05-18+）

---

## セッション管理

- [ ] 無関係タスク間に `/clear` を使用しているか
- [ ] 長い探索には `use subagents to investigate X` を使っているか
- [ ] 2回以上同じ修正をしたら `/clear` して新しいプロンプトで再開しているか
- [ ] Checkpoint を活用してリスクある変更を試みているか（`/rewind` または `Esc+Esc`）
- [ ] 長期タスクは `/rename` でセッション名をつけて管理しているか

---

## 並列実行・スケール

- [ ] 独立したタスクはバックグラウンドエージェント（`--bg`）で並列実行しているか
- [ ] CI/CD には `claude -p "prompt" --allowedTools` を使用しているか
- [ ] Fan-out パターンで大規模マイグレーションを自動化しているか
- [ ] `/goal` コマンドで条件達成まで自律継続させているか（v2.1.139+）
- [ ] `claude agents` でバックグラウンドセッションを一覧管理しているか（v2.1.139+）

---

## 品質・安全

- [ ] Claude に自己検証手段（テスト・スクリーンショット・expected output）を提供しているか
- [ ] CLAUDE.md に「IMPORTANT」「YOU MUST」で重要ルールを強調しているか
- [ ] 過去に Claude が犯した同じミスがあれば CLAUDE.md に追記したか
- [ ] 検証なしにコードをシップしていないか（「信頼して検証」ルール）

---

## 新機能チェック（2026-05-15時点）

- [ ] `/goal` コマンドで完了条件を設定した自律実行を試したか
- [ ] `claude agents` でバックグラウンドセッションを管理しているか
- [ ] Auto Mode（`Shift+Tab`）で繰り返し承認を省略しているか
- [ ] `/ultrareview` で大規模 PR のレビューを自動化しているか
- [ ] `/ultraplan` で複雑な計画を自動化しているか
- [ ] Fast Mode（`/fast`）が Opus 4.7 になったことを確認したか

---

## 新機能チェック（2026-05-25時点）

### サブエージェント強化
- [ ] `/agents` コマンドでサブエージェントをインタラクティブに作成・管理しているか（2026-05-25+）
- [ ] サブエージェントに `memory: project` を設定してセッションをまたいだ知識蓄積をしているか（2026-05-25+）
- [ ] `disallowedTools` で拒否ツールのdenylistを使い分けているか（`tools` との違いを理解しているか）（2026-05-25+）
- [ ] `permissionMode` でサブエージェントの権限を適切にスコープしているか（2026-05-25+）
- [ ] `isolation: worktree` で独立gitワークツリー内でサブエージェントを実行しているか（2026-05-25+）
- [ ] `skills` フィールドで起動時にスキルをプリロードしているか（2026-05-25+）
- [ ] `mcpServers` でサブエージェント専用のMCPサーバーをスコープしているか（2026-05-25+）
- [ ] `--agent <name>` フラグでセッション全体を特定サブエージェントとして動作させているか（2026-05-25+）
- [ ] `@"agent-name (agent)"` メンションで特定サブエージェントを明示呼び出ししているか（2026-05-25+）
- [ ] フォークモード（`CLAUDE_CODE_FORK_SUBAGENT=1`）で並列実験を試しているか（2026-05-25+）
- [ ] サブエージェント再開（「Continue that code review...」パターン）を活用しているか（2026-05-25+）
- [ ] `Agent(worker, researcher)` 構文でオーケストレーターの生成範囲を制限しているか（2026-05-25+）

### フック強化
- [ ] `if` フィールドでフックの実行条件を絞っているか（例: `"Bash(rm *)"`）（2026-05-25+）
- [ ] SessionStart フックで `CLAUDE_ENV_FILE` に環境変数を書き込んでいるか（2026-05-25+）
- [ ] `watchPaths` を SessionStart フックで設定して `FileChanged` イベントを有効化しているか（2026-05-25+）
- [ ] `InstructionsLoaded` フックでCLAUDE.md のデバッグをしているか（2026-05-25+）
- [ ] `prompt` タイプフックでAIによる動的判断ゲートを実装しているか（2026-05-25+）

### エコシステム
- [ ] `/plugin` でプラグインマーケットプレイスを確認したか（2026-05-25+）
- [ ] typed言語プロジェクトにコードインテリジェンスプラグインをインストールしたか（2026-05-25+）
- [ ] Agent Teams（`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`）を大規模タスクで試したか（2026-05-25+）

### セッション・コンテキスト管理
- [ ] `claude --continue` / `claude --resume` でセッションを再開しているか（2026-05-25+）
- [ ] `/compact Focus on the API changes` などカスタム指示付き圧縮を使っているか（2026-05-25+）
- [ ] `/rewind` の "Summarize from here" / "Summarize up to here" を使い分けているか（2026-05-25+）
- [ ] Worktrees で複数Claudeセッションを安全に並列実行しているか（2026-05-25+）
- [ ] `Ctrl+B` でタスクをバックグラウンド化しているか（2026-05-25+）
- [ ] コンテキストウィンドウ可視化ページでスタートアップコストを把握しているか（2026-05-25+）

### CLAUDE.md・メモリ
- [ ] モノレポで `claudeMdExcludes` を設定して無関係な CLAUDE.md をスキップしているか（2026-05-25+）
- [ ] `~/.claude/rules/` でプロジェクト横断の個人ルールを管理しているか（2026-05-25+）
- [ ] `.claude/rules/` でシンボリックリンクを使ってルールを複数プロジェクトで共有しているか（2026-05-25+）

---

## 新機能チェック（2026-06-01時点）

### サブエージェント強化
- [ ] `statusline-setup` / `claude-code-guide` など7種のビルトインサブエージェントの用途を理解しているか（2026-06-01+）
- [ ] Explore/Plan サブエージェントが CLAUDE.md と git status をスキップすることを理解しているか（コスト削減）（2026-06-01+）
- [ ] CI/CDスクリプトで `--agents` フラグを使ってセッション限定サブエージェントを定義しているか（2026-06-01+）
- [ ] プラグイン提供エージェントの識別子（`plugin:subdir:name` 形式）を使いこなしているか（2026-06-01+）
- [ ] `--add-dir` ディレクトリはサブエージェントスキャン対象外であることを把握しているか（2026-06-01+）

### フック強化
- [ ] `Setup` フックイベントで一回限りの初期化処理を定義しているか（2026-06-01+）
- [ ] SessionStart フックで `sessionTitle` / `initialUserMessage` / `reloadSkills` を活用しているか（2026-06-01+）
- [ ] `mcp_tool` タイプフックでMCPサーバーツールをフックから直接呼び出しているか（2026-06-01+）
- [ ] `prompt` タイプフックに `model` フィールドを指定してモデルを選択しているか（2026-06-01+）
- [ ] フックが並列実行されること・重複ハンドラーは自動排除されることを把握しているか（2026-06-01+）
- [ ] プラグイン内フックで `${CLAUDE_PLUGIN_DATA}` プレースホルダーを活用しているか（2026-06-01+）

### 品質保証
- [ ] 長時間自律実行後に逆境的レビューパターン（Adversarial Review）でサブエージェントに差分をレビューさせているか（2026-06-01+）
- [ ] ビルトインの `/code-review` スキルで実装後の品質チェックを自動化しているか（2026-06-01+）

### メモリ・設定
- [ ] `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` でAuto Memoryを環境別に無効化しているか（2026-06-01+）
- [ ] `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` で `--add-dir` ディレクトリのCLAUDE.mdをロードしているか（2026-06-01+）
- [ ] 組織展開時に `claudeMd` 設定キーでCLAUDE.md内容をmanaged-settings.jsonに埋め込んでいるか（2026-06-01+）
- [ ] 自動化スクリプトで `--append-system-prompt` フラグを使ってシステムプロンプトを追加しているか（2026-06-01+）
- [ ] `--setting-sources` フラグでロードする設定ソースを必要に応じて制限しているか（2026-06-01+）

---

## 新機能チェック（2026-06-08時点）

### フォーク機能（v2.1.161+: デフォルト有効）
- [ ] `/fork <指示>` で現在の会話履歴を引き継いだサブエージェントを起動しているか（2026-06-08+）
- [ ] フォークパネルで `Enter` を使ってフォークに追加指示を送れるか（2026-06-08+）
- [ ] `CLAUDE_CODE_FORK_SUBAGENT=0` で意図せずフォークが発生するのを防止しているか（不要な場合）（2026-06-08+）
- [ ] `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` でバックグラウンドタスクを無効化できることを知っているか（2026-06-08+）

### 新規フックイベント活用
- [ ] `Stop` フックを決定論的ゲートとして実装し、テスト・ビルドがパスするまでブロックしているか（最大8回制限あり）（2026-06-08+）
- [ ] `MessageDisplay` フックでアシスタントメッセージの表示を変換・フィルタリングしているか（タイムアウト10秒）（2026-06-08+）
- [ ] `Elicitation` フックでMCPサーバーの入力要求を自動処理しているか（2026-06-08+）
- [ ] `StopFailure` フックで `rate_limit` や `authentication_failed` などのAPIエラーを監視しているか（2026-06-08+）
- [ ] `WorktreeCreate` / `WorktreeRemove` フックでワークツリーのライフサイクル管理をしているか（2026-06-08+）
- [ ] `PostCompact` フックで圧縮後のコンテキストを補完しているか（2026-06-08+）
- [ ] `TaskCreated` / `TaskCompleted` フックでタスク管理を自動化しているか（2026-06-08+）
- [ ] `agent` フック型（実験的）でフックから検証サブエージェントをスポーンしているか（2026-06-08+）

### フック設定の新フィールド活用
- [ ] フック入力の `agent_id` / `agent_type` でサブエージェントコンテキストを識別しているか（2026-06-08+）
- [ ] `UserPromptSubmit` フックのタイムアウトが30秒に短縮されていることを把握しているか（2026-06-08+）
- [ ] `CLAUDE_EFFORT` 環境変数をフック内で活用しているか（2026-06-08+）

### サブエージェント管理
- [ ] `CLAUDE_CODE_SUBAGENT_MODEL` 環境変数で全サブエージェントのモデルを一括オーバーライドしているか（2026-06-08+）
- [ ] `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` でサブエージェントの自動圧縮しきい値を調整しているか（デフォルト95%）（2026-06-08+）
- [ ] Task ツールを Agent ツールに移行したか（`Task(...)` はエイリアスとして動作継続、v2.1.63+）（2026-06-08+）
- [ ] サブエージェントファイルの直接編集後にセッション再起動が必要であることを認識しているか（2026-06-08+）
- [ ] `disable-model-invocation: true` のスキルはサブエージェントの `skills` フィールドでプリロードできないことを知っているか（2026-06-08+）

### CLAUDE.md・メモリの新知識
- [ ] CLAUDE.md がシステムプロンプトではなくユーザーメッセージとして注入されることを理解しているか（2026-06-08+）
- [ ] 重要な指示はプロジェクトルートの CLAUDE.md に置いているか（圧縮後に再注入される、ネストファイルは非自動）（2026-06-08+）
- [ ] `CLAUDE_CODE_NEW_INIT=1` のインタラクティブ `/init` を試したか（2026-06-08+）
- [ ] 厳密に守らせたい指示には `--append-system-prompt` フラグまたはフックを使っているか（2026-06-08+）

### よくある失敗パターンのチェック（公式ドキュメント追加）
- [ ] 無関係なタスクを1セッションで混在させていないか（「キッチンシンクセッション」）（2026-06-08+）
- [ ] 同じミスを2回以上修正していたら `/clear` してより具体的なプロンプトで再開しているか（2026-06-08+）
- [ ] CLAUDE.md が長すぎて重要ルールが埋もれていないか（必要ない行は削除またはフックに移行）（2026-06-08+）
- [ ] Claude の実装を検証手段なしに信頼していないか（テスト・スクリプト・スクリーンショットで確認）（2026-06-08+）
- [ ] 調査スコープを絞らずに無限探索させていないか（サブエージェントに委任してメインコンテキストを保護）（2026-06-08+）

---

## 新機能チェック（2026-06-08 第2回更新時点）

### モデル・コスト最適化
- [ ] **Opus 4.8** を使っているか（v2.1.154+、デフォルト high エフォート）（2026-06-08+）
- [ ] `/fast` で Opus 4.8 の高速モード（2倍コスト・2.5倍速）を使っているか（2026-06-08+）
- [ ] `/effort xhigh` で最難タスクに最大努力を設定しているか（2026-06-08+）
- [ ] `fallbackModel` を設定してプライマリモデル障害時の可用性を確保しているか（v2.1.166+）（2026-06-08+）
- [ ] `MAX_THINKING_TOKENS=0` または `--thinking disabled` でルーティンタスクのコストを削減しているか（v2.1.166+）（2026-06-08+）
- [ ] `CLAUDE_CODE_SUBAGENT_MODEL` で全サブエージェントモデルを一括オーバーライドしているか（開発時コスト削減）（2026-06-08+）

### ダイナミックワークフロー
- [ ] `/effort ultracode` で大規模並列ワークフローをトリガーしているか（v2.1.154+）（2026-06-08+）
- [ ] `/workflows` でバンドルワークフロー（Deep Research 等）を確認したか（2026-06-08+）
- [ ] `.claude/workflows/` ディレクトリでカスタムワークフローを管理しているか（2026-06-08+）

### セキュリティ・エンタープライズ
- [ ] `requiredMinimumVersion` / `requiredMaximumVersion` で組織のバージョン範囲を管理しているか（v2.1.163+）（2026-06-08+）
- [ ] SendMessage でクロスセッションメッセージングのセキュリティ制限（リレーされた権限リクエストを拒否）を理解しているか（v2.1.166+）（2026-06-08+）
- [ ] シェルスタートアップファイル（`.zshenv` 等）への書き込み確認プロンプトを認識しているか（v2.1.160+）（2026-06-08+）
- [ ] `deniedTools: ["*"]` などglobパターンをdenyルールに使っているか（v2.1.166+）（2026-06-08+）

### フック強化（2026-06-08 第2回）
- [ ] Stop/SubagentStop フックで `additionalContext` を返してClaudeへフィードバックしているか（v2.1.163+）（2026-06-08+）

### プラグイン・スキル
- [ ] `.claude/skills` ディレクトリにプラグインを置いて自動ロードさせているか（v2.1.157+）（2026-06-08+）
- [ ] `claude plugin init <name>` で新しいプラグインをスキャフォールドしているか（v2.1.157+）（2026-06-08+）
- [ ] `/plugin list` でインストール済みプラグインを管理しているか（v2.1.163+）（2026-06-08+）

### エージェント管理
- [ ] `claude agents --json` の `waitingFor` フィールドでブロック原因を特定しているか（v2.1.162+）（2026-06-08+）
- [ ] Bedrock/Vertex/Foundry 環境で `CLAUDE_CODE_ENABLE_AUTO_MODE=1` でAuto Modeを有効化しているか（v2.1.158+）（2026-06-08+）

---

## 新機能チェック（2026-05-18時点）

### CLAUDE.md / メモリ
- [ ] CLAUDE.md に HTML コメント（`<!-- ... -->`）でメンテナーノートを書いているか（トークン節約）
- [ ] 他のエージェントフレームワーク（Cursor等）と共存する場合、`@AGENTS.md` インポートを設定したか
- [ ] `autoMemoryDirectory` でAuto Memory の保存場所をカスタマイズ検討したか
- [ ] サブエージェントに独自の Auto Memory を持たせて知識を蓄積させているか

### フック
- [ ] `asyncRewake: true` パターンでCIビルド・テスト完了通知を設定したか
- [ ] 頻繁に使う初期化フックに `once: true` を追加して重複実行を防いでいるか
- [ ] `PermissionRequest` フックで権限ダイアログを自動承認/拒否しているか
- [ ] `PreCompact` フックで重要な情報を圧縮前に保存しているか

### UX・セッション管理
- [ ] `/btw` コマンドで確認クエリをコンテキスト非汚染で行っているか
- [ ] 大型機能の設計前に `AskUserQuestion` パターンでClaude にインタビューさせているか
- [ ] Web と CLI を使い分ける場合、`$CLAUDE_CODE_REMOTE` 環境変数でフックを分岐しているか
- [ ] Chrome 拡張機能でUI変更の自動検証を設定したか
